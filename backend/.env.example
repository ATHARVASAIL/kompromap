ENVIRONMENT=development
DEBUG=true

POSTGRES_USER=kompromap
POSTGRES_PASSWORD=kompromap
POSTGRES_DB=kompromap
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional — only needed for Phase 5 narrative generation
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Optional API key. Unset = no auth (fine for localhost).
# Set this for ANY deployment reachable off your own machine.
# Generate one with:  python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=
